var WIDTH_PX = 1280;
var HEIGHT_PX = 800;

var BASE = 8;
var NUM_ROWS = 5 * BASE;
var NUM_COLS = 8 * BASE;

var CELL_SIZE = HEIGHT_PX / NUM_ROWS;

module.exports = {
    WIDTH_PX,
    HEIGHT_PX,
    BASE,
    NUM_ROWS,
    NUM_COLS,
    CELL_SIZE,
};
